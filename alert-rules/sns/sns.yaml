groups:
  - name: routing-critical4sns-rules
    rules:
      - alert: TestRoutingCritical4SNS
        expr: vector(1)
        for: 0m
        labels:
          severity: critical4sns
          alertname: TestCriticalSNS
        annotations:
          summary: "Тест маршрута CRITICAL4SNS (SNS)"
          description: "EMAIL critical4sns Этот алерт должен уйти в ресивер WL-critical4sns-condition (SNS Email/SMS)."

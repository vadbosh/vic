groups:
  - name: testing
    rules:
      - alert: TestAlertAlwaysFire
        expr: vector(1) and (minute() % 2 == 0)
        for: 0m
        labels:
          severity: test
        annotations:
          summary: "Тестовый алерт (всегда активен) ZZZ"
          description: "     Этот алерт нужен для проверки интеграции с Telegram. Если вы это читаете — всё работает."
